# Supabase — server-side only (no NEXT_PUBLIC_ prefix, so the anon key is
# never shipped to the browser). Set these in Vercel → Project → Settings →
# Environment Variables, and locally in a .env.local file.
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR-ANON-KEY
